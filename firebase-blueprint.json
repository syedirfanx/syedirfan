{
  "entities": {
    "Post": {
      "title": "Blog Post",
      "description": "Main post document containing meta information like likes.",
      "type": "object",
      "properties": {
        "likesCount": {
          "type": "integer",
          "description": "Number of likes for the post."
        }
      },
      "required": ["likesCount"]
    },
    "Comment": {
      "title": "Post Comment",
      "description": "A comment left by a visitor on a post.",
      "type": "object",
      "properties": {
        "authorName": {
          "type": "string",
          "description": "The name provided by the visitor."
        },
        "content": {
          "type": "string",
          "description": "The text content of the comment."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Server timestamp of when the comment was posted."
        }
      },
      "required": ["authorName", "content", "createdAt"]
    }
  },
  "firestore": {
    "posts/{postId}": {
      "schema": "Post",
      "description": "The root post document interaction node."
    },
    "posts/{postId}/comments": {
      "schema": "Comment",
      "description": "A subcollection of broadcasts for a specific post."
    }
  }
}
